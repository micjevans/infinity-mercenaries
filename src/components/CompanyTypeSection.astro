---
interface Props {
  title: string;
  tone?: "setup" | "benefits" | "restrictions" | "notes";
}

const { title, tone = "notes" } = Astro.props;
---

<section class:list={["company-type-section", `tone-${tone}`]}>
  <h3>{title}</h3>
  <div>
    <slot />
  </div>
</section>
